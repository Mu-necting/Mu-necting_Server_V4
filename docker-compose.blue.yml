#blue
version: '3'
services:
  munecting-develop-api:
    build: .
    ports:
      - "8081:8080"
    container_name: spring-blue
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    networks:
      - munecting-network

networks:
  munecting-network:
    external: true